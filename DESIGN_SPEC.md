# 穿越火线灵敏度测试调试助手——完整设计方案

本文档用于说明当前项目的产品目标、交互规则、计算逻辑、存储结构与验收标准。文档最后附有一份可以直接交给 Codex 的完整生成提示词。

## 1. 产品目标

制作两份完全独立、无需联网和构建工具的单文件网页：

1. CF 端游灵敏度调试助手：根据桌面鼠标操作习惯和三项测试成绩，输出 CF 端游当前设置界面可填写的鼠标移动速度与狙击镜移动速度。
2. CF 手游灵敏度调试助手：根据真实手机或平板触控习惯和三项触控测试，输出转向、开火、狙击一倍、非狙开镜四项建议值。

两份页面必须在本地浏览器直接运行，测试数据仅保存在浏览器 `localStorage`，不依赖后端，不读取或修改游戏文件。

## 2. 文件及运行环境

| 文件 | 目标设备 | 输入方式 |
|---|---|---|
| `CF灵敏度调试助手.html` | Windows / macOS 桌面浏览器 | 鼠标 |
| `CF手游灵敏度调试助手.html` | Android / iOS / HarmonyOS 手机或平板浏览器 | 触控 |

### 2.1 设备互斥

- 端游版检测到移动设备、粗指针触控设备或移动端 UA 后，显示全屏阻断层，禁用所有测试按钮，并提示改用手游版。
- 手游版未检测到移动 UA 或有效触控能力时，显示全屏阻断层，禁用所有测试按钮，并提示改用端游版。
- 不能仅用响应式布局代替设备限制。电脑缩小窗口仍然属于端游设备，手机请求桌面站点但具备触控能力时仍应优先判断为移动设备。
- 判断可综合 `navigator.userAgent`、`navigator.maxTouchPoints`、`matchMedia("(pointer:coarse)")` 和屏幕短边。

## 3. 通用视觉与交互

- 深色枪械科技风，主背景接近黑色，卡片使用深蓝灰色。
- 强调色使用 CF 风格橙红色，追踪目标使用青蓝色，完成状态使用绿色。
- 标题必须突出“CF 穿越火线”，但不能冒充官方产品。
- 页面顶部显示“本地浏览器运行 / 数据不上传”。
- 页面左上角固定显示 GitHub 仓库入口，滚动页面时保持可见；测试弹窗层级必须高于入口。
- 测试区域使用固定 32 × 32 像素等距网格，不能出现有大有小的网格。
- 动画必须由 `requestAnimationFrame` 驱动，不能使用低频定时器移动目标。
- 目标位置使用 `transform: translate3d(...)`，避免频繁修改布局属性造成卡顿。
- 测试倒计时使用 `performance.now()` 计算真实剩余时间，不能依赖递减整数，避免切换帧率后卡住。

## 4. 通用测试流程

### 4.1 开始

1. 用户点击“开始测试”。
2. 页面尝试调用 Fullscreen API。
3. 显示 2 秒准备弹窗，明确说明测试方式。
4. 准备结束后自动开始正式计时。

### 4.2 结束

1. 倒计时归零时立即停止计分和目标动画。
2. 计算本次成绩、疲劳指标以及该配置下的综合排名分。
3. 本次成绩优于历史成绩时替换当前最佳成绩，否则继续显示历史最佳成绩。
4. 显示完成弹窗，提供“直接进行下一项”和“立即关闭”。
5. 若用户不操作，10 秒后自动关闭测试弹窗并退出全屏。
6. 直接进入下一项时保持全屏，重新显示下一项的 2 秒准备提示。

### 4.3 多次测试

- 每项测试均允许反复测试。
- 推荐逻辑使用相同配置下的最好成绩，不简单使用最后一次成绩。
- 每个测试卡片都有“清除”按钮，只清除当前页面正在使用的该项成绩，不删除历史记录。
- 修改会影响结果的配置或时长后，当前结果应切换到新配置组合；若历史中存在完全相同的组合，则自动恢复该组合最好成绩，否则要求重新测试。

## 5. 疲劳修正

普通玩家测试时间越长越容易疲劳，不能把后段反应变慢完全解释为灵敏度不足。

### 5.1 追踪疲劳

- 把测试时间划分为 6 个等长时间桶。
- 分别统计前两个桶和后两个桶的加权追踪准确度。
- 疲劳值为 `max(0, 前段准确度 - 后段准确度)`，使用百分比显示。

### 5.2 定位与狙击疲劳

- 记录每次有效命中的反应时间和相对测试开始时间。
- 取前 40% 时间内的平均反应和后 40% 时间内的平均反应。
- 疲劳值为 `max(0, 后段平均反应 / 前段平均反应 - 1)`。
- 计算推荐值时，后段样本权重从约 0.65 平滑增加至 1.0，既反映疲劳又避免完全忽略后段稳定性。
- 疲劳明显时，微调建议应优先提示缩短单次测试、休息 30～60 秒，而不是直接提高灵敏度。

## 6. CF 端游版详细设计

### 6.1 用户档案

- 鼠标 DPI：默认 1100。浏览器不能自动读取鼠标驱动 DPI，页面必须明确说明并要求手动填写真实数值。
- 常用握法：默认趴握。
- 主要打法：默认狙击 / 瞬狙。
- 可用鼠标垫宽度：默认 30 cm。
- 分辨率使用方式：
  - 不修改，按当前系统自动计算。
  - 用户已手动修改系统分辨率，点击重新检测。
- 用于测试的分辨率。
- CF 游戏内分辨率。
- CF 灵敏度模式：
  - 统一灵敏度。
  - 水平 / 垂直独立。
- 三项自定义时长：默认追踪 30 秒、定位 45 秒、狙击 45 秒，允许 10～180 秒。

### 6.2 分辨率处理

- 浏览器只能读取 `screen.width`、`screen.height` 和 CSS 像素，不能修改 Windows 或 NVIDIA 控制面板分辨率。
- 页面应告诉用户先把系统分辨率调整为游戏常用分辨率，再重新检测。
- 浏览器因缩放、窗口边框或设备像素比报告出 1281 × 721 等临界值时，应与常用分辨率表比对，在横纵各误差不超过 1 像素时修正为 1280 × 720。
- 测试分辨率与游戏内分辨率必须分开选择。
- 每次重新进入页面后，若测试分辨率与游戏内分辨率不同，用户第一次点击任意测试卡片时必须显示一次换算提示。用户可按 1、2、3 任意顺序开始，提示逻辑不能只绑定第一项；取消提示时不开始测试，下次点击继续提示。用户确认后本次页面会话不重复提示，修改任一分辨率后重新触发。
- 推荐算法使用横纵分辨率比例或等效对角缩放因子，将测试空间的移动表现换算到游戏分辨率。
- 页面显著提示浏览器缩放应保持 100%。

### 6.3 三项测试

#### 微调追踪

- 默认 30 秒。
- 蓝色圆形目标连续平滑移动。
- 用户只需移动鼠标准星跟随目标，无需点击。
- 准星始终显示。
- 目标尺寸应较小但可辨识，避免过大导致追踪测试失真。
- 根据准星到目标中心距离使用分段权重累计：
  - 中心区域满分。
  - 外圈逐步衰减。
  - 超出判定范围记 0 分。
- 输出综合追踪准确度、中心保持率、横纵轴误差和疲劳值。

#### 快速定位

- 默认 45 秒。
- 橙色目标在安全边距内随机出现。
- 用户必须移动鼠标并点击目标，划过目标不计命中。
- 点空计为失误。
- 输出命中数、准确率、加权平均反应时间和疲劳值。

#### 狙击微调

- 默认 45 秒。
- 使用更小目标模拟开镜后的细微调整。
- 用户必须点击目标。
- 独立统计命中、失误、反应速度和疲劳，用于生成狙击镜移动速度。

### 6.4 端游输出

穿越火线端游设置重点只输出：

- 鼠标移动速度。
- 狙击镜移动速度。

统一模式显示两个整数值。水平 / 垂直独立模式显示：

- 鼠标水平灵敏度。
- 鼠标垂直灵敏度。
- 狙击镜水平灵敏度。
- 狙击镜垂直灵敏度。

所有结果必须限制在游戏设置允许填写的整数范围。推荐模块同时保存步枪、狙击、冲锋枪三种打法对应的统一方案和水平 / 垂直方案。

### 6.5 端游算法方向

最终值应综合以下变量：

- DPI 归一化因子，例如围绕 800～1100 DPI 使用平方根缩放，避免高 DPI 线性放大。
- 鼠标垫宽度和可用手部移动空间。
- 测试分辨率到游戏分辨率的换算因子。
- 追踪准确度和横纵误差。
- 定位命中率与加权反应时间。
- 狙击微调成绩。
- 握法和主要打法的小幅偏置。
- 疲劳指标，只做温和修正，不应压过真实成绩。

算法输出后必须 `Math.round`，再用 `clamp` 限制到游戏可用范围。页面必须声明这些是初始建议，不是官方换算公式。

## 7. CF 手游版详细设计

### 7.1 用户档案

- 操作指位：二指、三指、四指及以上。
- 持机方式：双手持机、单手持机、平板持握。
- 主要打法：狙击 / 瞬狙、步枪 / 综合、冲锋 / 近战。
- 屏幕对角线：默认 6.7 英寸。
- 触控采样率：默认 240 Hz，必须手动填写，网页不能自动读取。
- 三项时长：默认 30 / 45 / 45 秒，范围 10～180 秒。

### 7.2 触控测试

#### 手指追踪

- 用户必须按住测试区持续滑动，准星跟随触点。
- 抬手后停止累积有效追踪分。
- 不能用鼠标悬停替代触控。
- 计算追踪准确度、中心保持率和疲劳。

#### 快速定位

- 目标随机出现。
- 用户必须直接点按目标，点空记失误。
- 统计命中、失误、准确率、反应速度和疲劳。

#### 狙击微调

- 目标比快速定位更小。
- 必须轻触命中。
- 独立统计并主要影响狙击一倍灵敏度。

### 7.3 手游输出

输出 1～200 范围内的整数建议值：

- 转向灵敏度。
- 开火灵敏度。
- 狙击一倍灵敏度。
- 非狙开镜灵敏度。

同时计算步枪、狙击、冲锋三套方案。当前页面显示用户选择打法对应的一套，历史记录保存三套。

### 7.4 手游算法方向

基础转向值综合：

- 追踪准确度。
- 快速定位反应时间。
- 定位准确率。
- 触控采样率的平方根归一化。
- 屏幕尺寸平方根归一化。
- 操作指位、持机方式和打法偏置。
- 疲劳小幅修正。

开火灵敏度在基础转向上按打法调整；狙击一倍主要由基础转向与狙击微调反应共同决定；非狙开镜取基础转向的适度比例。不得把桌面 DPI、鼠标垫或 cm/360 直接套用到触控版。

## 8. 历史记录与数据结构

### 8.1 配置组合键

每个版本分别建立配置键。只有影响测试与推荐的字段完全一致时才属于同一组合。

端游示例：

```text
DPI|握法|打法|鼠标垫宽度|测试分辨率|游戏分辨率|灵敏度模式|追踪时长|定位时长|狙击时长
```

手游示例：

```text
指位|持机方式|打法|屏幕尺寸|采样率|追踪时长|定位时长|狙击时长
```

### 8.2 记录内容

```json
{
  "id": "时间戳-随机串",
  "key": "配置组合键",
  "config": {},
  "createdAt": 0,
  "updatedAt": 0,
  "results": {
    "track": {},
    "flick": {},
    "sniper": {}
  },
  "attempts": {
    "track": [],
    "flick": [],
    "sniper": []
  },
  "recommendations": {
    "rifle": {},
    "sniper": {},
    "rush": {}
  },
  "advice": ""
}
```

### 8.3 历史界面

- 不分页。
- 每种配置组合一个折叠模块。
- 标题显示配置和更新时间。
- 展开后显示三项最好成绩以及步枪、狙击、冲锋三套推荐。
- 页面启动、配置修改或数据导入后，自动查找当前配置组合。
- 找到时把历史最好成绩载入当前结果并恢复对应推荐。
- 单项“清除”不删除历史；“清空历史”必须二次确认。
- 导出格式为 UTF-8 JSON，包含版本号、导出时间和全部记录。
- 导入时校验基础结构，合并后按更新时间排序。

## 9. 性能和稳定性

- 所有动画使用单一 `requestAnimationFrame` 循环。
- 每帧 `dt` 设置上限，防止浏览器切换后台后一次性补偿过大。
- 倒计时从绝对时间差计算。
- 页面退出、测试结束或开始下一项时必须取消旧动画和计时器。
- 目标随机位置保留安全边距，避免目标被裁切。
- 移动端测试区设置 `touch-action: none`，避免滑动测试触发页面滚动或浏览器手势。
- 使用 Pointer Events 统一处理触控；追踪通过 `setPointerCapture` 保持连续输入。
- HTML 使用 UTF-8，不能出现中文乱码。

## 10. 验收标准

1. 两个 HTML 均可直接双击打开，不依赖 npm、CDN 或服务器。
2. 端游版在手机上阻断测试，手游版在普通 PC 浏览器阻断测试。
3. 每项开始前都有 2 秒说明和准备时间。
4. 倒计时连续变化，切换到高刷新率显示器也不会卡死。
5. 追踪动画平滑，网格统一为 32 × 32。
6. 端游追踪无需点击；端游定位和狙击必须点击。
7. 手游追踪必须按住滑动；手游定位和狙击必须点按。
8. 测试结束提供下一项按钮，10 秒后自动关闭并退出全屏。
9. 更差的重复成绩不会覆盖相同配置下的最好成绩。
10. 单项清除只清当前数据，历史记录仍存在。
11. 刷新或重新打开页面后历史记录仍可查看。
12. 导出后清空历史，再导入文件，可以恢复记录和当前配置最好成绩。
13. 修改任何影响结果的选项后，不会错误复用另一配置的成绩。
14. 端游最终值只使用游戏设置中存在的鼠标移动速度和狙击镜移动速度。
15. 手游最终值为转向、开火、狙击一倍、非狙开镜四项 1～200 整数。
16. 两版左上角 GitHub 仓库入口在页面滚动时固定可见，测试时被测试层完整覆盖。
17. 端游测试分辨率与游戏分辨率不同时，重新打开页面后的首次任意测试会提示一次。

## 11. 可直接交给 Codex 的生成提示词

将下面整段提示词连同本设计文档交给 Codex，可以从零重建或继续完善项目：

```text
请在当前工作目录创建一个名为“穿越火线灵敏度测试调试助手”的纯前端项目。项目必须只有独立可运行的 HTML/CSS/JavaScript 文件，不使用框架、不使用 npm、不使用 CDN、不需要服务器。

请完整阅读 DESIGN_SPEC.md，并严格按其中的功能、设备互斥、测试流程、疲劳修正、历史数据结构和验收标准实现：

1. 创建 CF灵敏度调试助手.html，服务 CF 端游桌面鼠标用户。
2. 创建 CF手游灵敏度调试助手.html，服务真实手机/平板触控用户。
3. 端游版禁止在移动端测试；手游版禁止在 PC 端测试，必须显示明确阻断提示。
4. 端游三项测试为微调追踪、快速定位、狙击微调。追踪无需点击；后两项必须点击。
5. 手游三项测试为按住滑动追踪、点按快速定位、点按狙击微调，不能沿用鼠标悬停逻辑。
6. 每项测试开始前显示 2 秒操作说明；测试结束显示下一项按钮，10 秒无操作自动关闭并退出全屏。
7. 动画使用 requestAnimationFrame，倒计时使用 performance.now，目标使用 translate3d，网格严格统一为 32×32。
8. 支持自定义时长、普通玩家疲劳修正、多次测试取最好成绩、单项清除当前数据。
9. 使用 localStorage 按配置组合保存历史，支持展示、导入、导出、全部清除；导入或重新打开页面后自动恢复当前配置最好成绩。
10. 端游输出 CF 设置中实际存在的鼠标移动速度与狙击镜移动速度，并支持统一及水平/垂直模式。
11. 手游输出 1～200 整数范围的转向、开火、狙击一倍和非狙开镜灵敏度。
12. 两版历史中都保存步枪、狙击、冲锋三套推荐与微调建议。
13. 明确说明网页不能读取鼠标驱动 DPI、不能修改系统分辨率、不能读取手机触控采样率，禁止伪造自动检测结果。
14. 页面为醒目清爽的 CF 深色科技风，中文 UTF-8 无乱码，适配目标设备。

实现后请：
- 对每个 HTML 的脚本执行 JavaScript 语法检查。
- 检查所有静态 DOM ID 引用存在。
- 检查没有重复运行的 requestAnimationFrame 或未清理的倒计时。
- 按 DESIGN_SPEC.md 第 10 节逐项给出验收结果。
- 不要把建议值描述为穿越火线官方公式。
```
